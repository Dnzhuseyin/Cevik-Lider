#!/usr/bin/env node

/**
 * Build script - ARTIK GEREKLİ DEĞİL
 * 
 * NOT: Artık Vercel API Proxy kullanılıyor (api/groq-proxy.js)
 * API key backend'de güvenli şekilde saklanıyor, frontend'e inject edilmesine gerek yok.
 * 
 * Bu script sadece uyumluluk için bırakıldı, çalışmıyor.
 */

console.log('ℹ️  Build script çalıştırıldı, ancak artık gerekli değil.');
console.log('✅ API Proxy kullanılıyor: /api/groq-proxy');
console.log('✅ API key backend\'de güvenli (Vercel Serverless Function)');
console.log('✅ Build script başarıyla tamamlandı (hiçbir şey yapmadı)');
